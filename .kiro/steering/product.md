# Product

A crew-facing order management panel for a food service queue system. Staff enter an order number to mark it as "ready", which writes to a shared Firestore database. The system is scoped to a specific branch (`EDSA_KAMIAS`) and prevents duplicate order entries.
